#include "main.h"


char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
int i, j;
extra = 1;

for (i = 0; n1[i] != '\0'; i++)
	;
for (j = 0; n2[j] != '\0'; j++)
	;

if (i > size_r || j > size_r)
	return(0);
i--;
j--;

while (i >= 0 || j >= 0)
{




}

}
